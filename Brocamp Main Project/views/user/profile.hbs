<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/homestyle.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css">
</head>

<body>
   

    {{> header}}
     <input type="hidden" value="{{cartCount}}" id="cartCount">

    <script>
        console.log('console is working for this page ')
    </script>

    <script>


        document.addEventListener('DOMContentLoaded', () => {
            const count = document.getElementById('cart-count')?.textContent || '0';
            const cart = document.getElementById('cartCount').value || 0
            console.log('this is the value from backend', cart)
            console.log('This is the value from the header:', count);
            document.getElementById('cart-count').textContent = cart
        });

    </script>

    <section class="breadcrumb-option">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="breadcrumb__text">
                        <h4>Profile</h4>
                        <div class="breadcrumb__links">
                            <a href="/user/home">Home</a>
                            <span>Profile</span>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <style>
        .details-container {
            margin-top: 60px;
            width: 75%;
        }

        .details-card {
            background-color: #F5F5F5;
            border-radius: 10px;
            padding: 30px;
            margin-top: 5px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }

        /* Heading Styling */
        .details-card h4 {
            font-family: 'Arial', sans-serif;
            font-weight: 700;
            color: #333;
            margin-bottom: 30px;
        }

        /* User Information Styling */
        .user-info {
            font-size: 1.2rem;
            font-family: 'Arial', sans-serif;
            color: #555;
            line-height: 1.5;
        }

        .user-info span {
            font-weight: 600;
            color: #333;
        }

        /* Button Styling */
        .btn-custom {
            font-size: 1rem;
            font-weight: 600;
            padding: 10px 20px;
            margin-top: 30px;
            width: 180px;
            border: none;
            border-radius: 5px;
            transition: all 0.3s ease;
        }

        /* Edit Button */
        /* Button Styling */
        .btn-custom {
            font-size: 1rem;
            font-weight: 600;
            padding: 12px 25px;
            margin-top: 30px;
            width: 200px;
            border: none;
            border-radius: 25px;
            transition: all 0.3s ease;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
            cursor: pointer;
        }

        /* Edit Button */
        .btn-edit {
            background-color: #4c91e2;
            color: white;
        }

        .btn-edit:hover {
            background-color: #3b7cbf;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.15);
        }

        /* Change Password Button */
        .btn-password {
            background-color: #e25656;
            color: white;
        }

        .btn-password:hover {
            background-color: #c14848;
            box-shadow: 0px 6px 12px rgba(0, 0, 0, 0.15);
        }

        .btn-address {
            background-color: #28a745;
            /* Customize this color */
        }

        .btn-logout {
            background-color: #dc3545;
            /* Customize this color */
        }
    </style>

    <script>
        document.addEventListener("DOMContentLoaded", () => {
            const x = document.getElementById('successmessage');
            console.log("Element found:", x); // Confirm element presence

            if (x) {
                setTimeout(() => {
                    x.style.display = "none";
                    console.log('Element hidden after 3 seconds');
                }, 3000);
            }
        });
    </script>
 

    <div class="container-fluid   ">
        <div class="details-card">
            {{#if success}}
            <h4 id="successmessage" class="text-center" style="color: green;">{{success}}</h4>
            {{/if}}
         

           
     <div class="user-info-card">
    <div class="user-info-header">
        <h2 class="user-info-title">User Information</h2>
        <p class="user-info-subtitle">Your profile details</p>
    </div>
    <div class="user-info">
        <div class="info-item">
            <span class="info-label">Username:</span>
            <span class="info-value">{{user.username}}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Email:</span>
            <span class="info-value">{{user.Email}}</span>
        </div>
        <div class="info-item">
            <span class="info-label">Phone Number:</span>
            <span class="info-value">{{user.phonenumber}}</span>
        </div>
    </div>
    <div class="user-info-footer">
        <form action="/user/editprofile/{{user.id}}" method="get">
         <button class="edit-btn" type="submit">Edit Profile</button>
        </form>
       
    </div>
</div>
<style>
     
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

/* User info card container */
.user-info-card {
    background-color: #ffffff;
    border-radius: 10px;
    box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
    padding: 25px;
    width: 350px;
    margin: 30px auto;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

/* Hover effect on card */
.user-info-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.1);
}

/* Header of user info section */
.user-info-header {
    text-align: center;
    margin-bottom: 20px;
}

/* Main title */
.user-info-title {
    font-size: 1.8rem;
    font-weight: 600;
    color: #333;
    margin-bottom: 5px;
}

/* Subtitle under title */
.user-info-subtitle {
    font-size: 1.1rem;
    color: #888;
}

/* User info items container */
.user-info {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    color: #444;
    margin-top: 20px;
}

/* Each information item container */
.info-item {
    display: flex;
    justify-content: space-between;
    margin-bottom: 15px;
    padding: 8px 0;
    border-bottom: 1px solid #eaeaea;
}

/* Styling for the label */
.info-label {
    font-weight: 600;
    color: #555;
    font-size: 1rem;
}

/* Styling for the value */
.info-value {
    font-weight: 400;
    color: #007BFF;
    font-size: 1rem;
    word-wrap: break-word;
}

/* Hover effect on value */
.info-value:hover {
    text-decoration: underline;
    cursor: pointer;
}

/* Footer with edit button */
.user-info-footer {
    display: flex;
    justify-content: center;
    margin-top: 20px;
}

/* Edit button */
.edit-btn {
    background-color: #007BFF;
    color: white;
    font-size: 1rem;
    padding: 10px 20px;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
}

/* Hover effect for the edit button */
.edit-btn:hover {
    background-color: #0056b3;
}

/* Optional: Responsive design */
@media (max-width: 600px) {
    .user-info-card {
        width: 90%;
    }

    .user-info-title {
        font-size: 1.5rem;
    }
}

</style>

            <!-- Button Container with Flex Classes -->
            <div class="d-flex flex-wrap justify-content-center gap-2 mt-3">
                {{!-- <form action="/user/editprofile/{{user.id}}" method="get">
                    <button class="btn btn-custom btn-edit" type="submit">Edit</button>
                </form> --}}
                <form action="/user/changepassword/{{user.id}}" method="get">
                    <button class="btn btn-custom btn-password" type="submit">Change Password</button>
                </form>
                <form action="/user/address/{{user.id}}" method="get">
                    <button class="btn btn-custom btn-address" type="submit">Address</button>
                </form>
                <form action="/user/logout" method="get">
                    <button class="btn btn-custom btn-logout">Logout</button>
                </form>
                <form action="/user/myorders/{{user.id}}" method="get">
                    <button class="btn btn-custom btn-logout">My orders</button>
                </form>

                <form action="/user/viewcoupun/{{user.id}}" method="get">
                    <button class="btn btn-custom btn-logout">Coupun</button>
                </form>

                <form action="/user/wallet/{{user.id}}" method="get">
                    <button class="btn btn-custom btn-logout">Wallet</button>
                </form>

            </div>
        </div>
    </div>


  {{> footer}}


</body>

</html>